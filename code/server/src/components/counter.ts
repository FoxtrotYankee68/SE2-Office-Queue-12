/**
 * Represents a counter of the office
 */
class Counter {
    id: string;
    name: String;

    constructor(id: string, name: string) {
        this.id = id;
        this.name = name;
    }
}

export { Counter}